const firstNum = +prompt('Введіть перше число');
const secondNum = +prompt('Введіть друге число');
const thirdNum = +prompt('Введіть третє число');

const res = (firstNum + secondNum + thirdNum) / 3;
alert(`Середнє значення: ${res}`);
